---
source: 2405.15793-swe-agent-agent-computer-interfaces-enable-automated-software.md
paper: SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering — Yang, Jimenez, Wettig, Lieret, Yao, Narasimhan, Press 2024 — arXiv:2405.15793
read: partial (Abstract; §1–§3 ACI concept + SWE-agent design; §4 setup incl. main + ablation tables; §5.1 Analysis of ACI Design; §5.2 Agent Behavior; App. §8.1 ACI Design — file viewer/editor/search; App. §9.1 Hyperparameter Sweep; §9.4 Failure Modes; §9.5 Pass@k; §9.9 exit conditions; App. §10 collapsed-observation + demonstration templates; App. §11 case study sympy-21614. Skipped: related work, per-repo/temporal breakdowns, n-gram analysis, HumanEvalFix detail, most qualitative trajectories.)
extracted_by: claude-opus-5[1m], 2026-08-22
---

## Core results

All ablation numbers below are **% Resolved on SWE-bench Lite (300 instances), SWE-agent w/ GPT-4
Turbo (`gpt-4-1106-preview`), default config = 18.0 %**, from
Table `tab:combined_ablations_summary` (reproduced in §4, discussed in §5.1). The ablations appear to
be single runs; the only variance measured is for the default config — 6 runs gave 17.33–18.67,
mean 17.94, σ 0.49 (App. §9.5, Table `tab:passk`). So a 3 pp delta is ≈6σ of that noise; the 1.7 pp
demonstration delta is not comfortably outside it.

1. **File-viewer window size — 100 lines is a peak, both directions hurt.** 30 lines → **14.3 %
   (↓3.7)**; 100 lines → **18.0 %**; whole file → **12.7 % (↓5.3)**. Measured. §5.1: "agents are
   sensitive to the number of lines the file viewer displays. Either too little content … or too
   much … lowers performance." Corroborated on the independent sweep (App. §9.1, n=37 dev
   instances): for GPT-4 Turbo a 200-line window loses to 100 in all four temperature/history
   pairings (9.2/10.8/8.7/10.8 vs 14.1/15.1/10.8/12.4).

2. **Search result shaping — capped-and-summarised beats paging, and paging is worse than having no
   search tool at all.** Summarised (SWE-agent's `search_dir`/`search_file`/`find_file`, hard-capped
   at 50 hits; over the cap it reports nothing and tells the agent to narrow the query) → **18.0 %**.
   *Iterative* search (VSCode/Vim-style: results shown one at a time with `next`/`prev`) →
   **12.0 % (↓6.0)**. No search commands at all (plain shell `grep`/`find`) → **15.7 % (↓2.3)**.
   Measured; the mechanism in §5.1 is observational, not separately ablated: with many hits, "agents
   tend to look through every match exhaustively, calling `next` until each result has been
   inspected", exhausting the cost budget or the context window.

3. **Context management — collapsing observations older than the last 5 to a one-line stub is worth
   +3.0 pp.** Last-5 observations kept in full, older ones replaced by `Old output omitted (101
   lines)` → **18.0 %**; full history → **15.0 % (↓3.0)**. Measured. Stated purposes (App. §10):
   (a) fewer tokens per step and (b) removing *outdated* information — old directory listings and
   stale file content that no longer describe the repo after edits. The sweep (App. §9.1) is the
   paper's only Claude datapoint for this knob: **Claude 3 Opus**, Last-5 vs Full
   at window 100 → 8.1 vs 5.4 (temp 0.0) and 8.1 vs 7.4 (temp 0.2); at window 200 → 7.1 vs 7.0
   (temp 0.0) but 6.8 vs 8.1 (temp 0.2). Direction holds in 3 of 4 pairings; n=37, so this is weak.

4. **Edit linting — reject-on-syntax-error is worth +3.0 pp; having a structured edit command at all
   is worth +7.7 pp.** `edit` with linting → **18.0 %**; `edit` without linting → **15.0 % (↓3.0)**;
   no `edit` command (shell `sed`/redirection only) → **10.3 % (↓7.7)**. Measured. Implementation
   (App. §8.1): `flake8 --isolated --select=F821,F822,F831,E111,E112,E113,E999,E902` on the open
   file after every edit; if it fires the edit is **reverted** and the agent is shown the error code,
   what the edit *would* have looked like, and the original content. The three parts of that message
   were ablated qualitatively only: without the error type the agent misdiagnoses; without the
   would-be snippet it re-issues the same command; without the original snippet it edits against
   stale content from earlier turns.

5. **A demonstration in the prompt matters much less once the interface is good.** SWE-agent w/o
   demonstration → 16.3 % (↓1.7); Shell-only w/o demonstration → 7.33 % vs 11.00 % with (main
   table). Measured. Shaping the tool substitutes for teaching the agent about the tool.

6. **Editing failure is common and compounds.** 51.7 % of trajectories contain ≥1 lint-rejected
   edit; an edit attempt eventually succeeds 90.5 % of the time, but only 57.2 % after one failure
   (§5.2). "Failed Edit Recovery" accounts for 23.4 % of unresolved Lite instances (App. §9.4).

7. **Agents succeed quickly and fail slowly.** Resolved runs: median \$1.21 and 12 steps. Unresolved:
   mean \$2.52 and 21 steps. 93.0 % of resolved instances submit before exhausting the \$4 budget vs
   69.0 % of all instances; 14.3 % of `submit`-terminated instances resolve vs 3.1 % of
   budget-terminated ones (§5.2, App. §9.9). The authors' inference: raising the budget or token
   limit is "unlikely to substantially increase performance."

8. **Headline (context for the deltas).** SWE-agent w/ GPT-4 Turbo: 12.47 % full SWE-bench, 18.00 %
   Lite, \$1.59/\$1.67 avg cost per resolved instance; Shell-only on Lite 11.00 %, \$1.46; RAG
   1.31 %/2.67 %. SWE-agent w/ **Claude 3 Opus**: 10.46 % full, 13.00 % Lite, \$2.59/\$2.18. The
   intro's "10.7 percentage points more than the baseline agent" is against Shell-only *without*
   demonstration (7.33 %); against Shell-only *with* one the gap is 7.0 pp (§5's 64 % relative
   increase). The ACI did **not** cut cost per resolved instance (\$1.67 vs \$1.46) — it bought
   resolve rate, not spend.

## Method and setting (what was actually built/tested)

Task: resolve a real GitHub issue in a Python repo so the hidden tests pass (SWE-bench; Lite = 300
self-contained functional bug fixes). Agent: a **single continuous ReAct session per instance** —
thought + one command per turn, no sub-agents, no hand-offs, no multi-session pipeline. Horizon:
median 12 turns for resolved runs, ~21 for unresolved, hard-capped by a **\$4 per-instance budget**
(overrun auto-submits whatever edits exist). Models: GPT-4 Turbo `gpt-4-1106-preview` and Claude 3
Opus `claude-3-opus-20240229` (Llama 3 and DeepSeek Coder were tried and were "subpar", partly on
context size). **Nothing is trained** — every result is prompting plus a shell wrapper (bash commands
in a YAML config), so the intervention set is fully portable. Baselines: BM25 RAG (one-shot patch)
and Shell-only (raw Linux shell, per InterCode). Final ACI reached by manual dev-set trajectory
inspection plus a grid search over window size / history processing / temperature on 37 instances.
Open-source.

## Relevance to P1–P4

**P1 — Context is the cost.** Only indirect support: the paper reports dollars, never a token or
cache breakdown, and the ACI *raised* cost per resolved instance (\$1.67 vs \$1.46). Shaped output
here is not an economy measure — the 50-hit cap and the 100-line window are justified as *accuracy*
interventions, token saving a side effect. The only trade-off the authors price (App. §8.1) runs the
other way: the search cap "forces the model to generate another search query which can be an
expensive operation … a trade-off between keeping observations concise and making additional calls
to the base LM."

**P2 — Sessions grow; the longest dominate.** Direct and strongly supportive (result 7). In their
setting long is a *symptom*, not a mode of work: resolved runs finish in 12 steps and \$1.21;
budget-exhausted runs resolve at 3.1 % vs 14.3 % for self-submitted ones; the authors conclude
raising the cap would not help. The sympy-21614 case study (App. §11) is the archetype — the agent
found the right file, then scrolled `function.py` until it burned the budget and terminated with
nothing submitted. Transfer gap: their turns sit in one context with no compaction *and* no plan
file; our long sessions are scoped by a plan, so "long = failing" is a hypothesis to instrument
here, not an imported fact.

**P3 — Every session rebuilds the same picture.** Bears on this only by analogy, and mostly
negatively: SWE-agent is one session per task, so there is no hand-off and no evidence about what a
hand-off must carry. Its collapse-after-5 mechanism (result 3) is the *within-session* version of our
problem: dropping the *content* of old observations while keeping their *structure and order* (the
action trace stays; the file dumps go) is worth +3.0 pp — a shape claim, not a multi-session one.
Result 5 is the sharper P3 signal: with the good interface a worked demonstration bought only 1.7 pp,
versus 3.67 pp for the raw shell — **fixing the tool beat re-teaching the friction in the prompt.**

**P4 — What smaller reads cost in grounding.** This is the paper's best contribution to us and the
one place we get a number for "too small": a 30-line window costs **3.7 pp** against 100 lines —
about 70 % of the damage that dumping the whole file causes (5.3 pp). So the curve is genuinely
two-sided, and the authors did not find a monotone "less context is better". What the paper says goes
wrong when the window is too small, in descending order of evidentiary strength: (i) turns are spent
re-navigating — `scroll_down` chains are among the most common action triples from turn 2 onward
(App. §9.3.2), and the sympy-21614 case study shows a scroll loop consuming the entire budget;
(ii) the edit guardrail interacts badly with a narrow view — App. §8.1's `orientation_strategy`
example fails specifically "because the file snippet is not large enough to show the entirety of the
`basic_chain` implementation", forcing a multi-step edit order; (iii) edits made against content
seen several turns ago misfire, the reason the lint message re-shows the *original* snippet. Note
(i)–(iii) are mechanisms inferred from trajectories, not separately ablated.

## Interventions this paper supports

- **Wrap `Read` with a bounded default window (~100–200 lines) plus a structured header** — full
  path, total line count, "N lines above / N below", line numbers — and a `goto`-style jump rather
  than "read the file again with a different offset". Rests on result 1 (100 > full file by 5.3 pp;
  100 > 30 by 3.7 pp) and App. §8.1's point that stateless re-reads make the model redo offset
  arithmetic. **Loses:** wide orientation reads cost more turns, and too tight a window reproduces
  the scroll-loop failure — do not go below ~100 lines on this paper's strength. Our Read's
  2000-line default sits squarely on the "too much" side of their curve.
- **Cap and summarise search/grep output, and on overflow refuse rather than paginate.** Rests on
  result 2 — *paging* search results (12.0 %) was worse than **having no search tool at all**
  (15.7 %). Any wrapper that lets an agent walk results one at a time invites exhaustive enumeration;
  prefer ≤N summarised hits, or nothing plus "narrow your query". **Loses:** re-query round-trips,
  which the authors explicitly price as the cost of the guardrail.
- **Have edit-shaped tools verify and reject, returning error + attempted result + original.**
  Rests on result 4 (+3.0 pp for linting alone, +7.7 pp for the structured edit command) and the
  three-part message finding. Our gate is per phase; this is a per-*action* gate. **Loses:** it
  constrains edit ordering (the `orientation_strategy` case) and can block a correct intermediate.
- **Make tool wrappers write bulk output to a file and return a stub.** The only lever we have
  toward result 3 given no compaction and no harness access — collapse the *content* of superseded
  observations while keeping the action trace. **Loses:** anything needed again must be re-fetched;
  and stale-content removal was half the stated benefit, which a fresh-session pipeline already gets.
- **Spend on shaping the tool before spending on prompt text that teaches around the tool.**
  Rests on result 5. Relevant to our per-session re-learning of CLI argument shapes: the paper's
  evidence favours fixing the interface over documenting the friction in every dispatch.

## Applicability caveats

- **Models.** The entire ablation table is **GPT-4 Turbo (2023-11)**. The only Claude evidence is
  Claude 3 Opus in the main table and the n=37 sweep — and there Claude's *best* window/history
  setting was a three-way tie including `200/Full`, i.e. the collapse-and-narrow prescription was
  **not** cleanly best for Claude. Claude 3 Opus is several generations behind Opus 4/5; long-context
  handling has changed materially since.
- **Statistics.** Ablations are single runs on 300 instances; run-to-run σ for the default config is
  0.49 pp. The 3.0–7.7 pp deltas survive that; the 1.7 pp demonstration delta does not clearly.
- **Task type and horizon.** Self-contained single-issue bug fixes, ~12–21 turns, one agent, one
  session, an 18 % ceiling. Nothing here tests planning, review, hand-off, or multi-agent context
  economy.
- **Benchmark quirk.** The \$4 budget cap shapes results — for Claude 3 Opus on full SWE-bench,
  slightly more than half of episodes ended by hitting it rather than submitting. Deltas attributed
  to "worse grounding" may partly be "ran out of money sooner".
- **Nothing needs training.** Every intervention is a wrapper or a prompt, so implementability is
  not the constraint here — evidence quality is.

## Briefing check

The briefing filed this under S5 as **"the ACI ablation only"**, and that is exactly right: the
transferable content is Table `tab:combined_ablations_summary` plus the App. §9.1 sweep; the rest
(benchmark scores, n-gram trajectory analysis, HumanEvalFix) is not decision-relevant here. Two
guards against overstating it. First, the note must not become "smaller reads are better" — the
measured curve is two-sided, and a 30-line window costs 3.7 pp against 100, so this is evidence for
*shaped* reads, not *small* ones. Second, the ablation is GPT-4 Turbo only, and the paper's own
Claude 3 Opus sweep does not cleanly reproduce the window-100/Last-5 optimum (Claude ties at
`200/Full`) — so "collapsing old observations is worth ~3 pp for our agents" is not supported for a
Claude model by this paper. What it supports unambiguously, and is worth carrying verbatim: **an
interface that let an agent page through search results scored worse (12.0 %) than giving it no
search tool at all (15.7 %)**, and **an edit that self-lints and reverts is worth +3.0 pp.**
