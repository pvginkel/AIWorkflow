{
  "name": "dev",
  "version": "0.9.30",
  "description": "The kc-native slice-development pipeline. Skills: /dev:onboard, /dev:triage, /dev:plan-slice, /dev:run-slice, /dev:close-out, /dev:slice-dag, /dev:arch-design. Ships 10 agents and the kc-native run loop and plan loop, which drive a slice's phased plan.md to done unattended and write everything out of their scope to one close-out report per slice; targets KubeCoder projects (each project describes itself in .kubecoder/project.yaml).",
  "author": {
    "name": "Pieter van Ginkel",
    "email": "pvginkel@gmail.com"
  }
}
